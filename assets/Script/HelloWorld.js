cc.Class({
    extends: cc.Component,

    properties: {
        tutorial:{
            default:null,
            type:cc.Button,
        }
    },

    // use this for initialization
    onLoad: function () {
    },

    // called every frame
    update: function (dt) {

    },
});
